# Flask Tutorial

### Running the app

From Powershell:
`$env:FLASK_APP = "flaskr"`
`$env:FLASK_ENV = "development"`
`flask init-db`
`flask run`

From bash:
`export FLASK_APP=flaskr`
`$ export FLASK_ENV=development`
`flask init-db`
`$ flask run`
